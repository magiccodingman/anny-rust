namespace Anny.Tests
{
    /// <summary>
    /// Facts about the prepared model the suites run against. PlayMode assemblies cannot see the
    /// EditMode test helpers, so the few numbers they need live here too.
    /// </summary>
    public static class AnnyTestConstants
    {
        public const int Vertices = 13718;
        public const int Bones = 104;
        public const int Faces = 27420;
    }
}
