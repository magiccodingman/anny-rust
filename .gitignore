/target/
/output/
/.upstream/
/.cache/
/fixtures/
/.venv/
*.log
__pycache__/
examples/browser/pkg/
examples/csharp/bin/
examples/csharp/obj/
# data/ is deliberately NOT ignored: import and commit the pinned source assets.
# Do not ignore all *.safetensors: converted source assets must remain trackable.
examples/qualification/node_modules/
# build-native.sh writes this sidecar next to the plugin it stages. The digest that matters is
# recorded in the validation docs, so the file itself stays a local build artifact.
integrations/unity/package/Plugins/x86_64/libanny.so.sha256
